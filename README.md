# Fitness

## Secrets
```bash
dotnet user-secrets init --project Fitness
dotnet user-secrets set "Fitness:DBUser" "<dbuser>" --project "Fitness"
dotnet user-secrets set "Fitness:DBPW" "<dbpassword>" --project "Fitness"
```
